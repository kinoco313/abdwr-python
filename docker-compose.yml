services:
  marimo:
    build: .
    ports:
      - "2718:2718"
    volumes:
      - ./notebooks:/app/notebooks
      - ./docs:/app/docs
      - ./src:/app/src
      - ./data:/app/data
    environment:
      - MARIMO_SKIP_UPDATE_CHECK=1
